{% extends 'base.html.twig' %}

{% block title %}PhotoUser{% endblock %}

{% block body %}
    <h1>PhotoUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo_user.id }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ photo_user.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('photo_user_index') }}">back to list</a>

    {# <a href="{{ path('photo_user_edit', {'id': photo_user.id}) }}">edit</a> #}

    {{ include('photo_user/_delete_form.html.twig') }}
{% endblock %}
